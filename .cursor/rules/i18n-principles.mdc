---
description: i18n and multilingual implementation baseline
alwaysApply: true
---

# squid 多语言规则（i18n Baseline）

## 语言范围与默认值

- 支持语言固定为：`en`、`zh`、`ja`、`ru`、`it`、`fr`、`de`。
- 默认语言必须是 `en`。
- 缺失翻译时必须回退到 `en`，禁止直接渲染空文案或 key 原文。

## 运行时文案

- 用户可见文案（标签、提示、通知、占位符、帮助文本）必须走 i18n 资源，不要在业务逻辑中硬编码。
- 新增或修改 UI 文案时，需要同步维护至少英文词条，并保证其他语言结构键位对齐。
- 新增语言切换相关逻辑时，必须校验 locale 白名单，非法值回退 `en`。

## 注释与系统提示词

- 新增或修改的代码注释统一使用英文。
- 新增或修改的 system prompt / promptTemplate 统一使用英文。
- 若历史中文提示词被触达，应优先迁移为英文版本后再扩展能力。

## 文档多语言

- 文档按 `docs/locales/<locale>/` 组织多语言入口。
- 未完成翻译页面应明确回退英文，不阻断文档访问。
- 公共配置中应声明默认语言与回退语言（默认 `en`，回退 `en`）。

## 提交前自检

- 是否引入了新的硬编码用户文案？
- 是否破坏了 locale 白名单或默认/回退行为？
- 是否新增了非英文注释或非英文系统提示词？
- 文档新增内容是否遵守多语言目录结构与回退策略？
